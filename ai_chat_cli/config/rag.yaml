# ============ RAG 知识库配置 ============

chunk_size: 500             # 文本分割块大小（字符数）
chunk_overlap: 50           # 相邻块之间的重叠字符数
top_k: 3                    # 检索返回的最相关结果数量
db_dir: "knowledge_base"    # 向量库存储目录（相对于用户数据目录）
